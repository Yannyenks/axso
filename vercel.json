{
  "buildCommand": "rm -rf .next/cache && prisma generate && prisma db push --skip-generate && next build",
  "functions": {
    "app/api/ai/axia/**": { "maxDuration": 120 },
    "app/api/ai/**": { "maxDuration": 60 }
  }
}
